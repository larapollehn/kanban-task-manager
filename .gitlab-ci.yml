image: node:latest

stages:
  - lint
  - build
  - test

eslint:
  stage: lint
  script:
    - echo "Linting"
    - npm install eslint
    - eslint --init
    - eslint src/*.js

build:
  stage: build
  script:
    - echo "Building"
    - mkdir build
    - touch build/info.txt
  artifacts:
    paths:
      - build/

test:
  stage: test
  script:
    - echo "Testing"
    - test -f "build/info.txt"
