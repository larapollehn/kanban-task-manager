import {SET_TITLE} from "../actions/types";

const initialState = {
    title: "",
};

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;

    }
}